using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Planets
{
    /*
    public class FluidBody : OrbitalBody
    {
        public ParticleSystem fluid;
        public float size;
        
        public FluidBody() : base(system, type, name, mass, radius, revTime, vel, bodyRotAxis, bodyRotSpd, bodyDampAmt)
        {
        }

        // Use this for initialization
        void Start()
        {
            fluid = gameObject.GetComponent<ParticleSystem>();
            fluid.startRotation = {-180,180};
            fluid.maxParticles = 1000;
            fluid.emission.rateOverTime = 1000;
            fluid.shape.shape = sphere;
            fluid.shape.radius = 0;
            fluid.rotationOverLifetime = {-180,180};
            fluid.render.material = "Gas Surface";
            fluid.Play();
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
    */
}
