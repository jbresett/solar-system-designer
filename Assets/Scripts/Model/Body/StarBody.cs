using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Planets
{
    /*
    public class StarBody : OrbitalBody
    {
        /*
        public ParticleSystem flare;
        public float size;

        public StarBody() : base(system, type, name, mass, radius, revTime, vel, bodyRotAxis, bodyRotSpd, bodyDampAmt)
        {
        }

        // Use this for initialization
        void Start()
        {
            flare = gameObject.GetComponent<ParticleSystem>();
            flare.startRotation = {-180,180};
            flare.maxParticles = 1000;
            flare.emission.rateOverTime = 1000;
            flare.shape.shape = sphere;
            flare.shape.radius = 500;
            flare.rotationOverLifetime = {-180,180};
            flare.render.material = "Sun Surface";
            flare.Play();
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
    */
}
